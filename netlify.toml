# Dreamcoat — Project Code Registry
# Static site (just index.html) + serverless functions for shared storage.

[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"
